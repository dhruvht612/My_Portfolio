import HoverFooter from './ui/hover-footer'

function Footer({ navLinks, heroSocials, footerBadges }) {
  return <HoverFooter navLinks={navLinks} heroSocials={heroSocials} footerBadges={footerBadges} />
}

export default Footer
